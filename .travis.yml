language: scala

branches:
  only:
    - 2.13.x
    - 2.12.x
    - 2.11.x
    - 2.10.x
    - 2.9.x
    - 2.8.x

scala:
  - "2.13.0-M3"

jdk:
  - oraclejdk9
  - oraclejdk8
  - openjdk8

sudo: false

notifications:
  email:
    recipients:
      - marko.elezovic@oradian.com

script:
  # Try to build documentation and package sources
  - sbt ++$TRAVIS_SCALA_VERSION publishLocal

after_success:
  - bash <(curl -s https://codecov.io/bash)
